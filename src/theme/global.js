const global = {
  globalCodeColor: '#3f51b5',
  globalBackgroundColor: '#fefefe',
  globalSize: '100vh'
  // If you are using an `Navbar` ouside of an `hero` of Bulma, uncomment the globalSize below and comment the current one.
  // globalSize: 'calc(100vh - 3.25rem)'
  // 3.25rem is the size of the Bulma navbar.
  // I didn't implement variables import from sass files into Javascript to get this variable from Bulma,
  // as it would be a little too heavy just for this case.
}

export default global
